{% extends "index" %}
{% set pageTitle = entry.heroTitle %}
{% block content %}
	<div class="container mx-auto prose lg:prose-xl">
		<h1>{{ entry.heroTitle }}</h1>
		<p>{{ entry.heroText }}</p>
	</div>
{% endblock %}
